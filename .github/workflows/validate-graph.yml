name: Validate Brand Graph

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v3
      - name: Validate no cycles
        run: node scripts/validate-graph.js
